import Image from "next/image";
import { FaQuoteLeft } from 'react-icons/fa/index.js';
import { Navigation, Pagination } from "swiper";
import { Swiper, SwiperSlide } from "swiper/react";
import "swiper/css";
import "swiper/css/navigation";
import "swiper/css/pagination";

const testimonialData = [
  {
    image: "/t-avt-1.png",
    name: "Jesus Martinez",
    position: "Business Owner - United States",
    message:
      "Love the quick turnaround and great ability to be flexible. Also love the reasonable pricing, would definitely recommend.",
  },
  {
    image: "/t-avt-2.png",
    name: "Hiran Wijesekara",
    position: "Business Owner - Dubai",
    message:
      "I’m happy with Lara’s work. She delivered on time and the editing was solid. Communication was good, though sometimes I had to follow up to get updates. Still, I’d work with her again.",
  },
  {
    image: "/t-avt-3.png",
    name: "John Atun",
    position: "Client - Philippines",
    message:
      "I needed the project done quickly, and Lara managed to deliver despite the tight deadline. It wasn’t perfect on the first try, but she was willing to make adjustments fast. Appreciate her effort under pressure.",
  },
];

const TestimonialSlider = () => {
  return (
    <Swiper
      navigation
      pagination={{
        clickable: true,
      }}
      modules={[Navigation, Pagination]}
      className="h-[400px]"
    >
      {testimonialData.map((person, i) => (
        <SwiperSlide key={i}>
          <div className="flex flex-col items-center md:flex-row gap-x-8 h-full px-16">
            {/* avatar, name, position */}
            <div className="w-full max-w-[300px] flex flex-col xl:justify-center items-center relative mx-auto xl:mx-0">
              <div className="flex flex-col justify-center text-center">
                {/* avatar */}
                <div className="mb-2 mx-auto">
                  <Image
                    src={person.image}
                    width={100}
                    height={100}
                    alt={person.name}
                  />
                </div>

                {/* name */}
                <div className="text-lg text-accent">{person.name}</div>

                {/* position */}
                <div className="text-[12px] uppercase font-extralight tracking-widest">
                  {person.position}
                </div>
              </div>
            </div>

            {/* quote & message */}
            <div className="flex-1 flex flex-col justify-center before:w-[1px] xl:before:bg-white/20 xl:before:absolute xl:before:left-0 xl:before:h-[200px] relative xl:pl-20">
              {/* quote icon */}
              <div className="mb-4">
                <FaQuoteLeft
                  className="text-4xl xl:text-6xl text-accent/20 mx-auto md:mx-0"
                  aria-aria-hidden
                />
              </div>

              {/* message */}
              <div className="xl:text-lg text-center md:text-left">
                {person.message}
              </div>
            </div>
          </div>
        </SwiperSlide>
      ))}
    </Swiper>
  );
};

export default TestimonialSlider;
